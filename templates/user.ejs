<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title><%= product_name %> - <%= lang('User Settings') %> </title>
    <% include partials/meta %>
</head>
<body>
<% include partials/header %>
<div class="container">
    <div class="section">
        <div class="row">
            <div class="col s12 m6">
                <ul class="collection with-header">
                    <li class="collection-header"><h4><%= lang('Change Settings') %></h4></li>
                    <li class="collection-item">
                        <div class="collection-form">
                            <form id="formUsername">
                                <div class="input-field">
                                    <input class="mdl-textfield__input" type="text" id="newUserName" value="<%= user.FullName %>" required>
                                    <label class="collection-form-label" for="newUserName"><%= lang('Username') %></label>
                                </div>
                                <button type="submit" id="changeNameBtn" class="waves-effect waves-light btn collection-form-button"><%= lang('Change') %></button>
                            </form>
                        </div>
                        <div class="collection-form">
                            <form id="formUseremail">
                                <div class="input-field">
                                    <input class="mdl-textfield__input" type="text" id="email" value="<%= user.name %>" required>
                                    <label class="collection-form-label" for="email"><%= lang('Email address') %></label>
                                </div>
                                <button type="submit" id="changeMailBtn" class="waves-effect waves-light btn collection-form-button"><%= lang('Change') %></button>
                            </form>
                        </div>
                    </li>
                </ul>
                <ul class="collection with-header">
                    <li class="collection-header"><h4><%= lang('Change Password') %></h4></li>
                    <li class="collection-item">
                        <div class="collection-form">
                            <form id="formUserpassword">
                                <div class="input-field">
                                    <input class="mdl-textfield__input" type="password" id="oldPW" required autocomplete="off">
                                    <label class="collection-form-label" for="oldPW"><%= lang('Current Password') %></label>
                                </div>
                                <div class="input-field">
                                    <input class="mdl-textfield__input" type="password" id="password" required autocomplete="off">
                                    <label class="collection-form-label" for="password"><%= lang('New Password') %></label>
                                </div>
                                <div class="input-field">
                                    <input class="mdl-textfield__input" type="password" id="newRepPW" required autocomplete="off">
                                    <label class="collection-form-label" for="newRepPW"><%= lang('Repeat New Password') %></label>
                                </div>
                                <button type="submit" id="changePass" class="waves-effect waves-light btn collection-form-button"><%= lang('Change') %></button>
                            </form>
                        </div>
                    </li>
                </ul>
            </div>
            <div class="col s12 m6">
                <ul class="collection with-header">
                    <li class="collection-header"><h4><%= lang('Delete Account') %></h4></li>
                    <li class="collection-item">
                        <div class="collection-form">
                            <p><%= lang('Deletes all your Pads and Account Data') %></p>
                            <button id="btn-deleteAllData" class="waves-effect waves-light btn collection-form-button"><%= lang('Delete') %></button>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
<% include partials/footer %>
</body>
</html>
