<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title><%= product_name %> - <%= lang('Login') %></title>
    <% include partials/meta %>
</head>
<body>
<% include partials/header %>
<div class="container">
    <div class="section">
        <div class="row">
            <div class="col s12 m6 offset-m3">
                <ul class="collection with-header">
                    <li class="collection-header"><h4><%= lang('Login') %></h4></li>
                    <li>
                        <div class="collection-form">
                            <form id="formEtherpadLogin">
                                <div class="input-field">
                                    <input class="mdl-textfield__input" type="text" id="email" required>
                                    <label class="collection-form-label" for="email"><%= lang('Email address') %></label>
                                </div>
                                <div class="input-field">
                                    <input class="mdl-textfield__input" type="password" id="password" required autocomplete="off">
                                    <label class="collection-form-label" for="password"><%= lang('Password') %></label>
                                </div>
                                <button type="submit" class="waves-effect waves-light btn collection-form-button"><%= lang('Login') %></button>
                            </form>
                        </div>
                    </li>
                </ul>
                <% if (settings.allow_registration) { %>
                <a class="right" href="<%= baseurl %>register"><%= lang('Register') %></a>
                <% } %>
                <a href id="passwordreset"><%= lang('Password reset') %></a>
            </div>
        </div>
    </div>
</div>
<% include partials/footer %>
</body>
</html>
